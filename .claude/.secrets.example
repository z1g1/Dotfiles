# Copy this file to ~/.claude/.secrets on each host and fill in real values.
# Do NOT commit the real .secrets file — it is git-ignored.
#
# Required by ~/.claude/hooks/notify.sh for ntfy push notifications.
#
#   NTFY_TOKEN=<bearer token for your ntfy server>

NTFY_TOKEN=
